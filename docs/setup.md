# Setup

1. Install Python 3.11.
2. Create venv: `python -m venv .venv`
3. Activate venv.
4. Install package: `pip install -e ".[dev]"`
5. Copy `.env.example` to `.env`.
6. Run `python scripts/test_audio_devices.py`.
